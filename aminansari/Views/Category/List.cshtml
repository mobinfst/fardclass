@using aminansari.Dto
@model List<CategoryDto>

<div class="container">
    <h2>category List</h2>
    <a href="/category/add" class="btn btn-success">Add  Category</a>

    <table class="table table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>#</th>
            </tr>
        </thead>
        <tbody>

            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.Name</td>
                    <td>@item.Description</td>
                    <td>
                        <a href="/category/update?id=@item.Id" class="btn btn-warning">Edit</a>
                        <a href="/category/Delete?id=@item.Id" class="btn btn-danger" onclick="return confirm('آیا مطمئن هستید؟')">Delete</a>
                    </td>
                </tr>
            }


        </tbody>
    </table>
</div>